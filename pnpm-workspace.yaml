packages:
  - "crates/ng-transform-napi"
  - "packages/*"
allowBuilds:
  # Ships prebuilt binaries; the build script is only a compile-from-source
  # fallback, and the watcher is unused in `vitest run` anyway.
  '@parcel/watcher': false
  esbuild: true
  unrs-resolver: true
