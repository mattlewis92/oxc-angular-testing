# Rust
/target
**/*.rs.bk

# Node
node_modules
.pnpm-store

# napi binaries (restored from CI artifacts at publish time)
*.node
*.wasm
# wasm/wasi loader shims — not built (no wasm target)
crates/ng-transform-napi/browser.js
crates/ng-transform-napi/*.wasi.cjs
crates/ng-transform-napi/*.wasi-browser.js
crates/ng-transform-napi/wasi-worker.mjs
crates/ng-transform-napi/wasi-worker-browser.mjs
# NB: index.js / index.d.ts (the napi loader + types) are generated by
# `napi build` but committed — the publish job has no build step, so they ship
# from the checkout. Regenerate (`pnpm --filter @oxc-angular-testing/transform build`)
# and commit when the #[napi] API changes.

# Build output
dist
*.tsbuildinfo

# OS / editor
.DS_Store
*.log

# insta pending snapshots
**/*.snap.new

# JetBrains IDE
.idea/
